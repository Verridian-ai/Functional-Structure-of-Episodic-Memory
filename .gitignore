# Large data files (exceed GitHub's 100MB limit)
corpus.jsonl

# Old folders (reorganized into new structure)
GWK LAW/
Readme Assets/
Notes docs/
GSW_docs/
prompts/
examples/
reports/

# IDE and task management
.cursor/
.taskmaster/
tasks/

# Root level duplicates (now in assets/ or docs/)
/Beyond Search Building a True Legal Reasoning Engine.png
/GSW Giving Language Model a Human Like Episodic Memory.png
/RAG VS GSW.png
/motivation.png
/operatorv2.png
/How_AI_Learns_to_Remember.mp4
/Consolidated_Documents.md
/Research Law Docs

# Videos in assets are allowed (for README)

# Old PDF files at root (now in docs/research/)
gsw_example*.pdf
GSW_pipeline*.pdf
GSW_QA_pipeline*.pdf

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE settings
.idea/
.vscode/
*.swp
*.swo
*~

# Temporary files
nul
*.tmp
*.log
*.bak

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment variables
.env
.env.local
*.env

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Data directories (large jsonl files)
data/domains/*.jsonl
data/processed/*.jsonl
data/by_type/*.jsonl
data/by_source/*.jsonl
data/by_court/*.jsonl

# Keep statistics files (small JSON)
!data/**/*statistics.json
!data/**/*_statistics.json

# Keep structure but ignore large files
!data/.gitkeep
!data/workspaces/.gitkeep
!data/domains/.gitkeep

# UI/Next.js
ui/node_modules/
ui/.next/
ui/out/
ui/.env.local

# Docker volumes
docker/langfuse/postgres_data/

# Large processed data files (keep extraction state)
data/processed/*.json
!data/processed/extraction_state.json
!data/processed/extraction_statistics.json
data/legislation/*.jsonl

# Keep workspace files (essential for system)
!data/workspaces/*.json

# Keep benchmark files
!data/benchmarks/*.json

# Keep validation files
!data/validation/*.json

# Keep legislation sections (not large)
!data/legislation/family_law_act_1975_sections.json

# Keep model checkpoints
!data/models/**/*

# Pytest cache
.pytest_cache/
